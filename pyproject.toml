[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proximie-senior-ml-challenge"
version = "0.1.0"
description = "Mock temporal action segmentation pipeline for the Proximie ML challenge"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
